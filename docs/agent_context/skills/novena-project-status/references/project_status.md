# Novena Project Status

## Current Stage

- Phase: Pre-Launch / Release Candidate.
- Status: The Gateway normal-polling log-volume defect is resolved in software and ready for canary monitoring. The end-to-end simulated Gateway, Hub telemetry, alerting and governed-control paths are locally verified; physical CM4 replay, plan-aware visible telemetry history and production-environment preflight remain release gates.

## Current Sprint Goal

Review and merge the Managed Gateway hardening changes, then run the three vertical CM4 hardware replays before widening the pilot.

## Open Release Gates

1. Validate offline buffering across process restart and reconnect on physical CM4 hardware.
2. Run factory-energy, cold-chain and facilities/HVAC CM4 hardware replays with representative equipment.
3. Validate governed write-back on representative devices, including power loss, key rotation, restore and emergency-disable recovery.
4. Configure and validate production Stripe webhooks/live billing and inbound WhatsApp security before paid rollout.
5. Verify production telemetry history access stays plan-aware while physical telemetry deletion remains governed by the global 90-day TimescaleDB retention policy.
6. Run production readiness against the real deployment environment, public TLS endpoints, broker and notification providers.
7. Qualify a 3–5 customer technical canary before the planned 20-customer cohort.

## Current Sources of Truth

- Documentation authority: `docs/README.md`.
- Hub architecture and protocol: repo-local skills under `.agents/skills/`.
- Gateway runtime: the Gateway repository's `ARCHITECTURE.md` and repo-local skills.
- Production operations: `docs/production_deployment_plan.md`, `docs/production_readiness_kit.md`, `docs/production_backup_restore.md` and feature-specific operator guides.

## Recent Activity Log

- 2026-09-11: Simplified Guided Setup discovery for first-time customers. Found equipment and template selection now remain the primary path; exact IP/port discovery is demoted below results into a clearly optional Advanced settings disclosure with customer-safe installer/IT guidance and no simulator terminology. Per-device IP, port and unit ID overrides are also hidden under Advanced connection settings, while the secure bounded endpoint check and validation/deployment gates remain unchanged. Validation passed all 33 guided-commissioning tests; no production configuration or dependency changes were required.
- 2026-09-10: Fixed the guided-discovery completed-state UI so HTMX polling is explicitly disabled once scanning finishes and the specific Modbus endpoint disclosure remains open during any final in-flight replacement. This prevents clicking the manual IP/port control from immediately collapsing it. Focused Guided Setup validation passed 25 tests.
- 2026-09-10: Separated guided Modbus TCP endpoint discovery from template validation. The bounded attached-interface scan now retains TCP-reachable endpoints even when generic unit/register probes cannot identify them, while Hub onboarding adds an exact customer-approved IPv4/port check for simulator and routed-device cases, lets operators confirm IP, port and unit ID before template validation, and continues to gate deployment on successful read-only template validation. Upgraded Gateway pymodbus from 3.8.0 to 3.8.6 to fix synchronous client construction in discovery/RPC worker threads, rejected loopback scan/manual targets, and added focused Hub/Gateway regressions. Validation passed 159 Gateway tests, 32 Hub guided-commissioning tests and the local stack readiness checks; physical CM4 redeployment and Retry scan replay remain required.
- 2026-09-01: Clarified telemetry retention as a two-layer product policy. Subscription plans control customer-visible chart/API/export history; physical PostgreSQL/TimescaleDB deletion remains governed by the current global 90-day retention policy. This is intentional so upgrades can reveal retained telemetry inside the larger plan window while downgrades shrink access immediately without requiring immediate row deletion. Added source-of-truth documentation, customer-facing pricing copy, code comments at telemetry query/export gates, and regression coverage for upgrade/downgrade visibility without physical deletion.
- 2026-08-31: Resolved excessive Gateway warning volume during normal Modbus polling without upgrading pymodbus. Replaced the protected connector's deprecated `BinaryPayloadDecoder` calls with pymodbus 3.8's supported register-conversion API while retaining signed/unsigned 8/16/32/64-bit values, floats, strings/bytes, bits/coils, all byte/word orders, scaling/offset/enums, timestamps, device identity and wide-range behavior. Added a five-minute duplicate warning/error window for remote copies only, concise exception summaries, and exemptions for critical records plus state, activation and configuration failures. Focused reproduction changed 42 two-point polls from 252 decoder warnings to zero. The actual Gateway then produced 69 samples/138 correct PostgreSQL points through Modbus TCP, MQTT, Redis and Celery with 21 bounded healthy startup/diagnostic log rows, zero decoder warnings/tracebacks, and online/fresh status during polling. In a separate stopped-simulator replay, 89 local warnings and 257 local errors became only three distinct remote fault rows while four separate startup/state rows remained visible; zero decoder warnings or tracebacks reached Hub. Validation passed 140 Gateway tests and 91 focused Hub telemetry/log/diagnostic/MQTT-identity tests (plus four subtests). Verdict: resolved with canary monitoring; CM4, RTU and physical-equipment replay remain required.
- 2026-08-28: Repeated the software-only Managed Gateway + Hub readiness audit. Added owner-authorized additional-team creation, closed authenticated cross-tenant access across all telemetry read/export surfaces, fixed governed-control PostgreSQL row locking against nullable templates, isolated command-signing tests from environment key rings, and corrected obsolete automatic-scan copy. The actual Gateway process polled a Modbus TCP simulator and delivered correct heartbeat, logs and 84 telemetry points through MQTT, Redis and PostgreSQL; first-customer service/live-MQTT canaries and a live threshold-alert-to-critical-ticket probe also passed. Validation passed 379 Hub tests with one expected skip, 124 Gateway tests, 19 alert/subscription tests, PostgreSQL-focused tenant/control tests, TimescaleDB hypertable verification and frontend type-check/build. The verdict remains Canary-ready, not broad-release ready: physical replay, production preflight, plan-aware visible telemetry history verification, and a high-volume deprecated pymodbus warning stream remain release gates.
- 2026-08-18: Reworked Gateway onboarding discovery into a signed, user-triggered scan. Hub now creates and correlates UUID scan IDs, renders idle/running/found/empty/error states with retry and manual fallback, rejects stale retry results, and gates validation/deployment. Gateway now defaults background scanning off, scans bounded directly attached private interfaces plus RTU adapters on demand, skips configured endpoints, publishes correlated progress/terminal reports, inventories unsupported USB/CAN, and uses the pinned pymodbus 3.8 keyword API. The hardware replay simulator/context and runbook now use click-to-scan with `10.0.0.20:502` manual fallback while preserving `NOV-AUDIT-FACTORY-HW` and Laptop 1 MQTT `192.168.100.7:1883`.
- 2026-08-18: Added the automated first-customer journey v1 canary. It creates an isolated marked customer/team, site, simulated claimed Gateway, device template/dashboard and device; defaults to ten serial-scoped QoS-1 MQTT readings through the existing MQTT consumer, Redis queue and Celery flush path; verifies persistence, freshness, authenticated sample readback, device dashboard widgets and Command Center state; and deletes successful-run database, cache and exact queue data. A deterministic service-ingestion mode supports CI, while strict non-production guards, 24-hour failed-run retention, exact/idempotent manual cleanup and focused coverage protect shared environments. The service-mode and live MQTT local canaries passed, the focused suite passed 7 tests, and post-run database/Redis checks found no FCJ residue.
- 2026-08-17: Re-audited production deployment/readiness artifacts against the current August Hub/Gateway state. Added `docs/production_deployment_plan.md` as the standalone VPS/cloud setup guide, refreshed `docs/production_readiness_kit.md` for managed Gateway hardening, serial-scoped MQTT, signed Guided Setup, governed control, Business Impact private reports, Command Center frontend assets, backup/restore expectations and current release gates, expanded `deploy/env/production.env.example` with newer production knobs, and updated current local-path docs from `/home/shouheng/projects/Novena-Hub` to `/home/shouheng/Novena-Platform/Novena-Hub`. Validation: diff whitespace passed, backup shell syntax passed, production Compose YAML parsed, and focused production-readiness/public-MQTT pytest passed (7 tests). Docker Compose runtime validation could not run locally because Docker is not installed in this WSL environment.
- 2026-08-14: Enabled customizable Command Center access for all teams and users through a Waffle data migration. The flag remains in place as an emergency operational kill switch, but is globally active by default after deployment migrations run.
- 2026-08-13: Expanded desktop Command Center panel resizing from a single corner to all four edges and all four corners, with directional cursors and visible corner affordances while retaining panel geometry limits and the mobile no-resize behavior.
- 2026-08-13: Fixed a Command Center navigation regression where clicking fixed KPI tiles could blank the live dashboard because the links inherited its HTMX partial-refresh target. KPI links now opt out of HTMX boosting, edit mode keeps users in the customization workflow until Save or Cancel, and focused rendering, TypeScript, and production Vite build checks passed.
- 2026-08-13: Implemented the feature-flagged customizable Command Center. The fixed headline KPI strip remains stable while operational panels can be reordered, resized on desktop, hidden, restored, cancelled, saved per user, reset, or published/removed as a team default by administrators. Added the versioned tenant-scoped layout model, registry/schema merging, strict geometry and revision validation, adaptive panel data, GridStack lifecycle integration, mobile keyboard/menu controls, ActivityLog events for team-default changes, and HTMX refresh isolation. Validation passed 66 dashboard/web Django tests, migration drift, Django checks, Ruff, TypeScript, the production Vite build, and Playwright interaction/visual checks at 1440x900, 1024x768, and 390x844. Browser/device canary evidence remains an operational gate.
- 2026-08-11: Reformatted the hardware replay guide again so copyable commands, values, expected outputs, troubleshooting notes and checklists are all in standalone fenced code blocks rather than embedded in tables or inline prose.
- 2026-08-11: Polished the Round 2 hardware replay guide for operator readability. Reformatted `docs/hardware_replay_runbook_2026-07-09.md` into copy/paste-safe station-by-station steps for Laptop 1, Laptop 2, Pi CM4 and browser onboarding, with cleaner command blocks, expected outputs, evidence checklist, troubleshooting table and scope notes.
- 2026-08-11: Added a CTO/CPO Physical Operations Platform roadmap artifact that stages Novena from the current pre-launch energy/cold-chain/facilities/PLC wedge into a mature APJC physical operations platform. The roadmap defines expansion gates across verticals, device families, protocols, workflows, AI, partner delivery, commercial packaging and leadership guidance while preserving the current strategy of evidence-backed templates and supervised canaries before broad claims.
- 2026-08-10: Merged Novena Gateway PR #10 (`codex/plc-datapoint-onboarding-p1` into `main`) so the CM4 hardware replay can clone the standard Gateway `main` branch. Updated the hardware replay guide Pi clone/pull commands back to `main`.
- 2026-08-10: Re-reviewed the Round 2 hardware/software replay preparation after the latest Hub/Gateway hardening and telemetry launch-readiness changes. The old hardware replay runbook was stale because the active workspace moved to `/home/shouheng/Novena-Platform`, the Pi flow now uses direct GitHub clone of Gateway `main` after PR #10 was merged, MQTT topics are serial-scoped, and secure Guided Setup now requires Gateway `trusted_clock` plus the Hub signing public key before `guided_setup_v1` is advertised. Updated the operator guide to cover Laptop 1 Hub/Mosquitto startup, local signing-key generation, Laptop 2 Modbus simulator, Pi CM4 Git clone/config/install/preflight, scoped MQTT evidence, and troubleshooting. Updated local-dev helper defaults from the old `/home/shouheng/projects/Novena-Hub` path to `/home/shouheng/Novena-Platform/Novena-Hub`.
- 2026-08-09: Implemented the real-time telemetry launch-readiness plan. Command Center is now an explicit near-live fleet summary with 15s/30s/60s HTMX refresh controls, a 60s default, manual refresh, local browser preference, last-refreshed copy and no device WebSocket subscriptions. Device detail remains the WebSocket inspection surface with effective plan/device intervals, polling fallback, pause/resume, manual refresh, selected chart windows, metric freshness, bounded ordered samples, metric filtering, visible CSV export and privacy-safe support diagnostics. Chart.js is bundled through the existing app asset instead of a CDN, typed history values and invalid history windows are handled safely, and focused rendering/consumer/API tests were added. Software checks passed; real-browser simulator and physical CM4 canaries remain release gates because no browser runtime or hardware is available in this workspace.
- 2026-08-09: Reviewed and polished the customer-facing real-time telemetry experience on the device detail page. Clarified browser stream state separately from device freshness, added pause/resume and manual refresh controls, made WebSocket stale/reconnect/polling fallback states customer-visible, bounded reconnect cleanup, deduped/out-of-order chart appends, highlighted latest sample/value updates, and preserved typed history values so boolean/status widgets render correctly before the next live packet. Validation passed telemetry pytest module, Django check, frontend type-check/build, Ruff on touched Python files, py_compile and diff whitespace checks on touched code. Playwright browser screenshot QA was not run because Playwright is not installed in this project.
- 2026-08-03: Re-ran the software-only Managed Gateway + Hub release-readiness audit across team/site lifecycle, claim/release/activation, guided discovery/configuration, telemetry/database/live alerts, diagnostics/logs/RPC, tenant isolation and subscription enforcement. Fixed unsafe Team API creation/deletion authorization and cascade deletion, blocked site deletion from bypassing secure Gateway release, deferred pilot MQTT publication until transaction commit, and enabled production Stripe webhook signature validation by default. Validation passed 356 Hub tests with one expected skip, 114 Gateway tests, real Mosquitto-to-Redis-to-PostgreSQL telemetry and alert/ticket probes, Mosquitto serial ACL tests, frontend type-check/build, Django/migration checks, Ruff and production-settings validation. Remaining software gaps are physical per-plan telemetry deletion, owner-facing additional-team creation, archived-Gateway site lifecycle, raw telemetry-key alert setup, production secret/service configuration and deprecated integrations; CM4 replay remains out of scope and required.
- 2026-08-01: Fixed the Novena Setup Assistant Setup Goal "Other" reveal behavior on the site personalization page. The Other checkbox now directly toggles the free-text input and the initializer runs immediately, making the behavior reliable in the Django/HTMX app shell. Validation passed focused onboarding tests, Django local check, Ruff on the touched test file and whitespace checks.
- 2026-07-30: Implemented P0 PLC/generic-I/O onboarding on a dedicated Hub/Gateway branch. Added a separate site-defined datapoint map with checksum-bound live validation and explicit confirmation, a user-friendly Modbus TCP/RTU signal editor, connection-only and decoded test reads, clone support, confirmed-only connector/dashboard generation, safe writable-intent blocking, and impossible-value checks. Fixed-function templates keep their existing path. Validation passed 213 affected Hub tests with one expected skip, 112 Gateway tests, Ruff, Django checks, migration drift checks, Gateway compilation, frontend production build and whitespace checks. Physical PLC/CM4 commissioning remains required before release.
- 2026-07-29: Removed the static "Gateway found", "Securing" and "Ready to scan" chips from the Novena Setup Assistant Gateway pairing page so customers do not mistake them for live progress before entering gateway details. Kept the page title/copy, Find the sticker helper panel, Gateway Name, Serial Number, Claim Code and Pair Gateway form intact. Validation passed focused onboarding tests, Django local check, Ruff on touched Python files and whitespace checks.
- 2026-07-29: Updated the Novena Setup Assistant connectivity step so Direct cloud connection remains visible as a Coming soon roadmap option, with helper copy and a disabled/non-selectable radio. Backend POST handling now rejects forged `connectivity=direct` submissions by returning to the connectivity step with a clear message instead of proceeding to Gateway setup; Novena Gateway remains the only selectable path. Validation passed focused onboarding tests, Django local check, Ruff on touched Python files and whitespace checks.
- 2026-07-29: Fixed the Novena Setup Assistant Setup Goal "Other" text input visibility so the free-text field is hidden by default with the native `hidden` attribute and only appears when Other is checked. Normalized onboarding context defaults for new sites so conditional rendering is stable. Validation passed focused onboarding tests, Django local check, Ruff on touched Python files and whitespace checks.
- 2026-07-29: Refined the Novena Setup Assistant site personalization step: removed the visible Site Type prompt, changed Setup Goal into multi-select checkboxes with an Other free-text field for product-discovery signals, added simplified Operating Hours with custom start/end time capture, made conditional inputs reveal reliably inside the HTMX app shell, and added `Site.metadata` for structured onboarding context. Validated focused onboarding tests, Django local check, no migration drift, local migration application/check, Ruff on touched Python files and whitespace checks; production readiness still fails for pre-existing local-dev production configuration gaps and missing `weasyprint`.
- 2026-07-29: Removed the manual timezone selector from the Novena Setup Assistant site personalization step. The onboarding form now submits a hidden browser-detected IANA timezone, and the server validates it with `zoneinfo` before falling back to existing site/user/project defaults. Focused onboarding tests, Django local check, Ruff on touched Python files and whitespace checks passed; production readiness command still fails for pre-existing local-dev production configuration gaps and missing `weasyprint`.
- 2026-07-29: Documented the preferred Novena GitHub workflow in the local development skill: keep local `git` for branches, diffs, staging and commits; prefer the connected GitHub plugin for PR creation, CI/check inspection, review handling and merges; use `gh` only as a fallback; never print or embed GitHub token values in commands or repository files.
- 2026-07-28: Restored the small vertical outcome-pack review fixes on a focused branch: General IoT is the default onboarding profile, risky cold-chain/energy wording is softened, General IoT template ranking stays neutral, and curated cold-room/HVAC templates plus focused onboarding tests cover the behavior. Validation passed focused onboarding tests, Django checks, migration drift check and Ruff on touched files. Larger outcome-pack refactor work was intentionally left out.
- 2026-07-28: Completed the bounded Managed Gateway technical hardening implementation and customer-facing polish: fail-closed verified credential revocation and fresh-row reclaim, automatic activation recovery, signed durable configuration delivery with exact acknowledgements, plan-safe polling reconciliation, clearer lifecycle/configuration labels, manager-friendly RPC history, and visible recovery actions. Validation passed 300 Hub tests with one expected skip and 13 subtests, 110 Gateway tests, Ruff, Django and migration checks, frontend production build, template compilation, and a real temporary Mosquitto Dynamic Security provision/revoke/idempotency smoke. Physical CM4 replay remains open.
- 2026-07-27: Completed a software-only Managed Gateway + Cloud Hub end-to-end review across team/site onboarding, claim and activation, status/freshness, signed config delivery, simulated telemetry, logs/RPC, failure handling, tenant isolation and plan enforcement. Safely hardened immutable Gateway identity/status editing, stale onboarding status copy, device-limit enforcement across alternate onboarding paths, ambiguous multi-device telemetry identity handling and the plan-limit upgrade link. Validation passed: 305 Django tests with one expected skip, 110 Gateway tests, frontend type-check/build, Django checks and migration check, three-persona pilot simulation/UI route smoke, and a real local MQTT broker-to-buffer-to-database telemetry probe. Controlled-canary gaps remain around fail-open credential deprovisioning on Gateway release, activation recovery after its 24-hour window, legacy config acknowledgement semantics, plan-safe edge polling and physical CM4 replay.
- 2026-07-27: Completed the operator-language product polish across remote-control safety, Gateway setup and diagnostics, Business Impact, alerts, maintenance, automations and directly affected public product copy. Added customer-safe translations for current and historical control states/errors while preserving stored values, permissions, calculations, audit exports and MQTT/RPC contracts. Validation passed: Django checks, no-migration check, 85 focused Django tests plus the alert pytest module, the complete 301-test Django suite with one expected skip, frontend build/type-check, pre-commit, documentation links and repository hygiene.
- 2026-07-26: Completed a cleanup-only Hub and Gateway hygiene pass: archived stale planning documents, clarified documentation authority, removed runtime snapshots from Git tracking, strengthened repository checks and verified 294 Hub tests plus 110 Gateway tests. No production modules, schemas, routes, MQTT/RPC contracts, Stripe binaries or protected ThingsBoard-derived paths changed.
- 2026-07-26: Merged Business Impact and ROI Release 1 behind its Waffle rollout flag, including versioned baselines, deterministic value calculations, reports, private storage/backup integration and production-readiness gates. The full Hub suite passed 293 tests with one expected skip after merge.
- 2026-07-25: Completed guided Modbus commissioning and governed remote-control security/reliability across Hub and Gateway. Physical CM4 and representative equipment validation remains mandatory.
- 2026-07-22: Closed Hub quick-add tenant-isolation gaps and stabilized the WSL-native test commands.
- 2026-07-21: Enforced serial-scoped inbound MQTT identity and topic/payload serial validation across Hub and Gateway.
- 2026-07-20: Hardened the current hardware replay runbook and reproducible field-test release process.
- 2026-07-18: Updated the CTO assessment, business plan and pilot gates; the 20-customer handout remains on hold pending technical canary evidence.
- 2026-07-15: Added the production readiness kit, deployment assets and backup/restore baseline.
- 2026-07-13: Verified timezone-safe customer displays and approved WhatsApp alert-template delivery.

Full historical activity through 26 July 2026 is retained at `docs/archive/project_activity_2026_h1/project_status_full_history_2026-07-26.md` and should only be loaded for historical investigation.

## 2026-08-14 Hardware Replay Operator Guide Simplification

- Added Laptop 1 hardware-test helper at `scripts/hardware-test/prepare_laptop1_hub.sh` to configure local MQTT on `1883`, generate/reuse the Guided Setup signing key, print the Gateway-facing public key, start/check local Hub services, and prepare the pilot audit fixture.
- Added Gateway config renderer at `install/hardware-test/render_local_replay_config.py` in Novena-Gateway to install `/etc/novena-gateway/config.json` from a short Pi command while preserving trusted Guided Setup signing keys and local MQTT `1883`.
- Rewrote `docs/hardware_replay_runbook_2026-07-09.md` as a three-machine operator guide with short commands, expected success signals, evidence checklist, and troubleshooting for MQTT, IP, Modbus, signature, and telemetry failures.
